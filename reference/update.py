#!/usr/bin/env python3

# Have some logic that updates the .hidden files, which are like manifests,
# with the names of the dotfiles and maybe something about where they're
# supposed to end up-- like a mapping of files with the absolute paths of their
# parent directories-- the FHS should make this pretty straight-forward.
